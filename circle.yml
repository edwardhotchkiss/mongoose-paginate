general:
  branches:
    only:
      - master
machine:
  timezone: America/New_York
  node:
    version:
      - 0.10
      - 0.12
  services:
    - mongodb
  environment:
    CODECLIMATE_REPO_TOKEN: "d492266813d4843606e969e1bcc8f8c7595bfea71a42d4c24027422cc90f9854"
#test:
#  override:
#    - "npm run-script test-circleci"
#  post:
#    - "npm install -g codeclimate-test-reporter && cat ./coverage/lcov.info | codeclimate"
